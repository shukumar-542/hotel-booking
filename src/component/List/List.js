import React from 'react';
import Header from '../Header/Header';
import Navbar from '../Navbar/Navbar';

const List = () => {
      return (
            <div>
                  <Navbar/>
                  <Header type="list"/>
            </div>
      );
};

export default List;